#!/bin/bash

echo ===Running the build hook===

docker build --target Installed -t nobodyxu/faiss:${IMAGE_NAME} .
docker build --target build -t nobodyxu/faiss:${IMAGE_NAME}-build .

echo ===Finish running the build hook===
